import { ReactNode } from 'react';
import { TemplateFn } from '../deck/deck';
import { SpectacleThemeOverrides } from '../../theme/default-theme';
export default function PrintMode({ children, theme, exportMode, pageSize, pageOrientation, backgroundImage, template }: PrintModeProps): JSX.Element;
declare type PrintModeProps = {
    children: ReactNode;
    theme?: SpectacleThemeOverrides;
    exportMode?: boolean;
    pageSize?: string;
    pageOrientation?: '' | 'landscape' | 'portrait';
    backgroundImage?: string;
    template?: TemplateFn | ReactNode;
};
export {};
//# sourceMappingURL=index.d.ts.map