import { CSSProperties } from 'react';
export declare function overviewFrameStyle({ overviewScale, nativeSlideWidth, nativeSlideHeight }: {
    overviewScale: number;
    nativeSlideWidth: number;
    nativeSlideHeight: number;
}): CSSProperties;
export declare function overviewWrapperStyle({ overviewScale }: {
    overviewScale: number;
}): CSSProperties;
export declare function printFrameStyle({ nativeSlideWidth, nativeSlideHeight, printScale }: {
    nativeSlideWidth: number;
    nativeSlideHeight: number;
    printScale: number;
}): CSSProperties;
export declare function printWrapperStyle({ printScale }: {
    printScale: number;
}): CSSProperties;
//# sourceMappingURL=deck-styles.d.ts.map